@model IEnumerable<Domaine.Team>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutCustom.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
   
        <th>
            @Html.DisplayNameFor(model => model.prManagerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.rHmanagerId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.adminEntId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        
        <td>
            @Html.DisplayFor(modelItem => item.prManagerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.rHmanagerId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.adminEntId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.teamId }) |
            @Html.ActionLink("Details", "Details", new { id=item.teamId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.teamId })
        </td>
    </tr>
}

</table>
